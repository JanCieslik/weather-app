export const toKelvin = (celsius) => (celsius + 273.15).toFixed(1);
export const toFahrenheit = (celsius) => ((celsius * 9/5) + 32).toFixed(1);
export const toCelsius = (celsius) => celsius.toFixed(1);